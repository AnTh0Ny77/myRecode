{% extends 'base.html.twig' %}
{% block style %}


{% endblock %}
{% block main %}


<main class="  d-flex align-items-center justify-content-center form-signin w-100 m-auto  text-center">
    <form method="POST" action="">
        {% if alert %}
        <div class="alert alert-warning my-3" role="alert">
            Identifiants invalides !
        </div>
        {% endif %}
        <div class="form-floating my-4 ">
                <input type="text" class="form-control" name="user__mail" value="{{alert['username']}}" placeholder="lexitjwtSecurity45">
            <label for="floatingInput">Username</label>
        </div>
        <div class="form-floating my-4">
            <input type="password" class="form-control" value="{{alert['password']}}" name="user__password"  placeholder="Password">
            <label for="floatingPassword">Password</label>
        </div>

        <button class=" btn btn-lg btn-primary mx-auto" type="submit">se connecter</button>
    </form>
</main>
{% endblock %}
{% block script %}

{% endblock %}